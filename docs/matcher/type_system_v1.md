---
tags: [matcher, types, phase2, diagnosis]
status: v1-draft
date: 2026-09-24
updated_by: メインClaude（CEO）
related: [[axes_v0]] [[questions_v0]] [[scoring_v0]] [[recommendation_v0]] [[web_only_strategy_v0]] [[series_plan_v0]]
---

# 子育てタイプ診断「わが家のこそだちタイプ」 タイプ体系 v1

> CEO決裁 2026-09-24（オーナー提案「診断を先に完成させ、タイプ別に記事を書く」を受諾）。
> 旧 `README.md` は「実親インタビュー後に実装」としていたが、**インタビュー（G1=20件）は読者不在で達成できていない**。
> 読者がいないから一次情報が取れず、一次情報がないから作れない、の循環を断つため**先に出す**判断に変更。
> 診断は**可逆**（気に入らなければ下げられる）・**追加コストゼロ**（静的サイト）なので、速く動かす対象。

## 1. なぜ診断なのか（事業上の役割）

現状のボトルネックは品質ではなく**リーチと読者基盤**（note 総PV 230・フォロワー1・Threads フォロワー0）。
記事は「読んだ人にしか効かない」が、診断には記事に無い3つの性質がある。

1. **共有される**：人は診断結果を人に見せる。フォロワー0でも、結果ページのURLは動く。
2. **入口が1つになる**：全SNSの導線を「まず診断」に集約でき、バラバラの投稿が1本の線になる。
3. **タイプが記事の器になる**：8タイプ＝8本の連載記事。書く理由と読む理由が同時に生まれる（オーナー提案の相乗効果）。

**やらないこと**: 正解を決める／親を格付けする／「あなたは○○型だから△△すべき」と指示する。
本診断は **現時点の傾向のスナップショット**であり、どのタイプも等価。ランキング表示はしない。

## 2. 軸：6軸 → 3軸に絞る（タイプ化のため）

`axes_v0.md` の6軸（R/S/I/E/P/C）は**分析には正しいが、タイプ名にすると 64 通りで覚えられない**。
MBTI が機能しているのは「4文字で自分を語れる」からなので、**タイプ化は3軸8タイプ**に絞る。

| 使う軸 | 略号 | 両極 | 絞った理由 |
|---|---|---|---|
| 主導権 | I | **導く**（I+） ⇔ **委ねる**（I−） | 親が最も自覚しやすく、理論の分かれ目そのもの |
| 構造性 | S | **整える**（S+） ⇔ **流れる**（S−） | 生活実感に直結（毎日の時間割）・回答がぶれにくい |
| 応答性 | R | **寄り添う**（R+） ⇔ **見守る**（R−） | 「抱っこしすぎ？」「待つべき？」という最頻の悩みの軸 |

**残る3軸（E 評価観 / P 早期化 / C 関係性）はタイプ名に使わず、結果ページの「サブ傾向」として1〜2行で添える。**
捨てるのではなく、タイプの粒度を上げずに個別性を出すために使う。

## 3. 8タイプ（v1・仮称）

命名はブランド（ひだまり＝場所・自然・温度）に合わせ、**人物の格付けではなく「場の風景」**で表す。
どのタイプにも「強み」と「つまずきやすい点」を必ず対で書く（片方だけにしない）。

| # | 軸 | タイプ名 | 一行の像 | 強み | つまずきやすい点 | となりに置く理論 |
|---|---|---|---|---|---|---|
| 1 | I+ S+ R+ | **灯台** | 先に光を置いて、そばにいる | 見通しと安心を同時に渡せる | 先回りしすぎて子の出番が減る | ボウルビィ／佐々木正美 |
| 2 | I+ S+ R− | **畑** | 種をまき、手をかけすぎずに育てる | 環境を整えて待てる | 「待つ」が「放置」に見える日がある | モンテッソーリ／ピクラー |
| 3 | I+ S− R+ | **たき火** | その日の風向きで、火を囲む | 状況に合わせて動ける・情が濃い | 基準が日によって変わり子が読めない | NVC／アドラー |
| 4 | I+ S− R− | **山道** | 先を歩き、ときどき振り返って待つ | 挑戦を促しつつ手は出しすぎない | 子のペースより道のりを優先しがち | 成長マインドセット／ヴィゴツキー |
| 5 | I− S+ R+ | **縁側** | 決まった場所で、いつでも隣に | 安心の土台が厚い・子が戻ってこられる | 子が離れていく時期に寂しさが出る | ボウルビィ（安全基地）／汐見稔幸 |
| 6 | I− S+ R− | **本棚** | 並べておいて、選ぶのは子 | 選択の自由と秩序を両立できる | 子の「今」の気持ちを拾い損ねる | モンテッソーリ／倉橋惣三 |
| 7 | I− S− R+ | **小川** | 流れに沿って、一緒に揺れる | 子の世界に入っていける・遊びが豊か | 生活リズムが崩れて親が疲れる | 汐見稔幸／遊びの理論 |
| 8 | I− S− R− | **野原** | 広く開けて、好きに走らせる | 子の自律性が育ちやすい | 危険と自由の線引きが後手になる | ピクラー／サドベリー（両論併記） |

> すべてのタイプ説明に「**この傾向は子の年齢・第二子・その日の余裕で変わります**」を明記する（固定化の防止）。

## 4. 質問：**各軸5問＝15問**（v1改訂 2026-09-24・オーナー指摘を受け 9問から増やす）

### なぜ9問では足りないか（測定の話・正直に）

心理尺度の信頼性（同じ人が測るたびに同じ結果になるか）は、**1つの軸に何問あるか**でほぼ決まる。

| 1軸あたりの問数 | 内部一貫性（目安 α） | 実例 |
|---|---|---|
| 2問 | 0.4〜0.6（弱い） | TIPI（10問でビッグファイブ5因子）＝簡便版。研究でも「精度は割り切る」前提 |
| 3問 | 0.6 前後 | v1初案（9問）。**結果が日によってぶれる** |
| **5問** | **0.7〜0.8（実用域）** | BFI-44 等の標準的な短縮版 |
| 20問超 | 0.8〜0.9 | MBTI 公式93問・IPIP-50 |

→ **各軸5問＝計15問**を採用。回答時間は約3分で、消費者向け診断としては一般的な長さ（12〜20問）に収まる。

**ただし正直に添えると**: 問数を増やせば「当たる」わけではない。MBTI は93問あっても、数週間後の再検査で**約半数の人のタイプが変わる**ことが知られている。
本診断も「性格の判定」ではなく **「いまの、わが家の傾向のスナップショット」** として提示する（この但し書きは結果ページに常時表示）。
問数を増やす目的は、**当てること**より **同じ人が何度やってもぶれないこと**と、**後から統計で検証できるだけのデータを取ること**にある。

### 内訳（計18問・約3分）

| 区分 | 問数 | 内容 |
|---|---|---|
| コア | 15（主導権5・構造性5・応答性5） | タイプ判定に使う。`questions_v0.md` の既存6問を核に、各軸3問を新規作成 |
| 文脈 | 3 | 子の年齢／きょうだい構成／**いまいちばんの悩み（自由記述・任意）** |
| 任意 | 0（v2で検討） | 評価観E・早期化P・関係性C は v2。完了率を見てから足す |

**自由記述「いまいちばんの悩み」が、実は最重要データ**。実親インタビュー（G1=20件）で取れなかった一次情報が、ここで蓄積する。記事テーマの選定を推測でなく実データで行えるようになる。

## 5. 実装：**静的サイト → DB付きサイトへ変更**（v1改訂 2026-09-24・オーナー指摘）

初案は「サーバー無し・回答を保存しない」だったが、**回答を保存しないと読者像が永久にわからない**。
インタビュー（G1=20件）が読者不在で達成できなかった以上、**診断が唯一の一次情報の入口**になる。よって保存する。

| 項目 | 決定 | 理由 |
|---|---|---|
| ホスティング | **Cloudflare Pages** | R2 で既に Cloudflare アカウントを使用中。GitHub Pages から変更し、DB と同じ基盤にまとめる |
| DB | **Cloudflare D1**（SQLite・無料枠 5GB/日次読み書き十分） | SQL で分析できる。Workers から数行で書ける。別アカウント不要 |
| API | Cloudflare Workers（`POST /api/response`） | 回答1件を1行 INSERT するだけ。認証不要・レート制限あり |
| 判定 | ブラウザ内で計算し、**結果表示は即時**。保存は裏で非同期 | 保存が失敗しても診断は動く（体験を壊さない） |
| 保存する項目 | 回答15問の生データ／算出タイプ／軸スコア／文脈3問／離脱した設問番号／日時・流入元（utm） | **生データを保存**することが肝。集計値だけ保存すると後から分析し直せない |
| 保存しない項目 | 氏名・メール・IP・Cookie による個人追跡 | 個人情報を持たない＝管理義務とリスクを負わない |
| 分析 | 私が `wrangler d1 execute` で SQL 集計し、`docs/insights/matcher.md` を定期生成 | 既存の insights ダッシュボードと同じ運用に乗せる |

### 分析でわかるようになること
1. **タイプ分布**（どんな親が来ているか）→ 記事の優先順位を実データで決める
2. **「いちばんの悩み」の頻出語**（自由記述）→ 次に書く記事テーマ／有料記事のテーマ選定
3. **設問ごとの離脱率** → 質問文の改善
4. **項目分析（各問と軸合計の相関・α係数）** → **停まっていた「専門家レビュー待ち」を実データで代替できる**。軸の妥当性を自前で検証・較正できる
5. 流入元別（Threads / IG / note / 直接）の完了率 → どのチャネルが本当に効いているか

### プライバシー（事業者としての義務）
- 匿名の回答のみを保存し、個人を特定できる項目は取らない。診断開始前に**一行の説明**（「回答は匿名で集計に使います」）と、
  **プライバシーポリシーのページ**を用意する（個人事業主として最低限必要）。
- 自由記述に個人情報が書かれる可能性があるため、公開時は**要約・頻出語のみ**を扱い、原文は公開しない。

## 6. 記事計画（在庫切れ 10/9 の次の柱）

連載「となりの考え方」は第9回まで在庫あり。**第10回以降をタイプ別8本**に充てる。

| 回 | 記事 | 対応タイプ |
|---|---|---|
| 予告編 | 「わが家の子育て、どの風景に近い？」＝診断の告知記事 | 全体 |
| 10〜17 | 各タイプの記事（強み・つまずき・となりの理論・明日試せること） | 灯台/畑/たき火/山道/縁側/本棚/小川/野原 |

既存の連載記事（01〜10）は各タイプの「となりに置く理論」として**内部リンクで再利用できる**＝過去記事が生き返る。

## 7. やらない・保留

- 50理論 × 6軸の推奨エンジン（`recommendation_v0.md`）は **MVP では使わない**。専門家レビュー未了のため、
  「あなたに合う理論を1位から並べる」形は出さない。**「となりに置いてみる理論を2つ紹介する」**に留める。
- × グループ（ヨコミネ式・七田式・公文式）は v0 決定どおり**推奨しない**（両論併記の対象としてのみ言及可）。
- 有料化はしない（診断・記事とも無料）。収益は別線で判断する。

## 8. リスクと歯止め

- **「決めつけられた」と感じさせる**risk → 全タイプ等価・ランキング無し・「スナップショット」明記・両論併記。
- **診断を出しても広まらない** risk → 診断は拡散の**器**であって起点ではない。起点（人に直接届ける＝note 誠実接触）は別途必要。
  **診断単体で読者が増えると期待しない**こと。
- **作り込みすぎて出ない** risk → MVP は 18問＋8タイプ＋結果ページ＋回答保存まで。**アカウント・ログイン・推奨エンジン・管理画面は作らない**（分析は私の SQL で足りる）。
