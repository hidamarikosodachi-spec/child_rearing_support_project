name = "hidamari-matcher-api"
main = "index.js"
compatibility_date = "2026-09-01"

# D1 は `wrangler d1 create hidamari-matcher` で作成し、出力された id を貼る
[[d1_databases]]
binding = "DB"
database_name = "hidamari-matcher"
database_id = "" # ← 作成後に記入

# Pages 側と同一オリジンで /api/* を受けるため、Pages Functions ではなく
# Worker Routes を使う場合はここに route を書く（初期は Pages の _routes で十分）
